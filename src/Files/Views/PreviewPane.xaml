<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.Views.PreviewPane"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Name="Root">

	<Border
		Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
		BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
		BorderThickness="1"
		CornerRadius="8">

		<Grid
			x:Name="PreviewSurface"
			Grid.Row="1"
			CornerRadius="8"
			PointerEntered="PreviewSurface_PointerEntered"
			SizeChanged="PreviewSurface_SizeChanged">
			<StackPanel
				x:Name="StatusPanel"
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				Spacing="8">
				<ProgressRing
					x:Name="LoadingIndicator"
					Width="28"
					Height="28"
					IsActive="False" />
				<TextBlock
					x:Name="StatusTextBlock"
					MaxWidth="260"
					HorizontalAlignment="Center"
					TextAlignment="Center"
					TextWrapping="Wrap" />
				<Button
					x:Name="PreviewAnywayButton"
					HorizontalAlignment="Center"
					Click="PreviewAnywayButton_Click"
					Visibility="Collapsed" />
			</StackPanel>

		</Grid>
	</Border>
</UserControl>
