<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.Views.AppearanceSettingsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:localization="using:Files.Localization"
	xmlns:views="using:Files.Views"
	xmlns:wctcontrols="using:CommunityToolkit.WinUI.Controls">

	<ScrollViewer
		Padding="36"
		HorizontalScrollBarVisibility="Disabled"
		HorizontalScrollMode="Disabled">

		<StackPanel Spacing="4">

			<TextBlock
				Margin="0,0,0,20"
				Style="{ThemeResource TitleTextBlockStyle}"
				Text="{localization:Localized ResourceKey=Appearance}" />

			<wctcontrols:SettingsCard Description="{localization:Localized ResourceKey=ThemeDescription}" Header="{localization:Localized ResourceKey=Theme}">
				<wctcontrols:SettingsCard.HeaderIcon>
					<FontIcon Glyph="&#xE790;" />
				</wctcontrols:SettingsCard.HeaderIcon>
				<ComboBox
					x:Name="ThemePicker"
					MinWidth="220"
					ItemsSource="{x:Bind Themes, Mode=OneTime}"
					SelectionChanged="ThemePicker_SelectionChanged">
					<ComboBox.ItemTemplate>
						<DataTemplate x:DataType="views:ThemeOption">
							<TextBlock Text="{x:Bind Label}" />
						</DataTemplate>
					</ComboBox.ItemTemplate>
				</ComboBox>
			</wctcontrols:SettingsCard>

		</StackPanel>

	</ScrollViewer>

</UserControl>
