<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.ItemProperties.ShortcutPropertyView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:localization="using:Files.Localization">
	<ScrollViewer Padding="14,12,14,8">
		<StackPanel Spacing="14">
			<Grid ColumnSpacing="12" RowSpacing="8">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<TextBlock Text="{localization:Localized ResourceKey=TargetType}" />
				<TextBlock
					Grid.Column="1"
					Text="{x:Bind Shortcut.TargetType}"
					TextTrimming="CharacterEllipsis" />
				<TextBlock Grid.Row="1" Text="{localization:Localized ResourceKey=TargetLocation}" />
				<TextBlock
					Grid.Row="1"
					Grid.Column="1"
					Text="{x:Bind Shortcut.TargetLocation}"
					TextTrimming="CharacterEllipsis" />
			</Grid>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock VerticalAlignment="Center" Text="{localization:Localized ResourceKey=Target}" />
				<TextBox
					Grid.Column="1"
					IsReadOnly="True"
					Text="{x:Bind Target}" />
			</Grid>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock VerticalAlignment="Center" Text="{localization:Localized ResourceKey=StartIn}" />
				<TextBox
					Grid.Column="1"
					IsReadOnly="True"
					Text="{x:Bind Shortcut.WorkingDirectory}" />
			</Grid>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock VerticalAlignment="Center" Text="{localization:Localized ResourceKey=ShortcutKey}" />
				<TextBox
					Grid.Column="1"
					IsReadOnly="True"
					Text="{x:Bind Hotkey}" />
			</Grid>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock VerticalAlignment="Center" Text="{localization:Localized ResourceKey=Run}" />
				<TextBox
					Grid.Column="1"
					IsReadOnly="True"
					Text="{x:Bind ShowCommand}" />
			</Grid>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock VerticalAlignment="Center" Text="{localization:Localized ResourceKey=Comment}" />
				<TextBox
					Grid.Column="1"
					IsReadOnly="True"
					Text="{x:Bind Shortcut.Comment}" />
			</Grid>
			<StackPanel Orientation="Horizontal" Spacing="8">
				<Button Content="{localization:Localized ResourceKey=OpenFileLocation}" IsEnabled="False" />
				<Button Content="{localization:Localized ResourceKey=ChangeIcon}" IsEnabled="False" />
				<Button Content="{localization:Localized ResourceKey=Advanced}" IsEnabled="False" />
			</StackPanel>
		</StackPanel>
	</ScrollViewer>
</UserControl>
