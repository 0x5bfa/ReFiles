<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.ItemProperties.CustomizePropertyView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="using:CommunityToolkit.WinUI.Converters"
	Loaded="UserControl_Loaded">

	<UserControl.Resources>
		<converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</UserControl.Resources>

	<ScrollViewer Padding="12,0,12,12">

		<StackPanel Spacing="14">
			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{x:Bind FolderQuestionLabel}" />
					<TextBlock Text="{x:Bind OptimizeFolderLabel}" />
					<ComboBox
						x:Name="FolderKindComboBox"
						HorizontalAlignment="Stretch"
						DisplayMemberPath="DisplayName"
						ItemsSource="{x:Bind FolderKinds}"
						SelectionChanged="FolderKindComboBox_SelectionChanged" />
					<CheckBox
						x:Name="ApplyToSubfoldersCheckBox"
						Click="ApplyToSubfoldersCheckBox_Click"
						Content="{x:Bind ApplyToSubfoldersLabel}" />
				</StackPanel>
			</Border>
			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}"
				Visibility="{x:Bind Customization.CanChangePicture, Converter={StaticResource BoolToVisibilityConverter}}">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{x:Bind FolderPicturesLabel}" />
					<TextBlock Text="{x:Bind FolderPicturesDescriptionLabel}" TextWrapping="Wrap" />
					<StackPanel Orientation="Horizontal" Spacing="8">
						<Button Click="ChooseFileButton_Click" Content="{x:Bind ChooseFileLabel}" />
						<Button Click="RestorePictureButton_Click" Content="{x:Bind RestoreDefaultLabel}" />
					</StackPanel>
				</StackPanel>
			</Border>
			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}"
				Visibility="{x:Bind Customization.CanChangeIcon, Converter={StaticResource BoolToVisibilityConverter}}">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{x:Bind FolderIconsLabel}" />
					<TextBlock Text="{x:Bind FolderIconsDescriptionLabel}" TextWrapping="Wrap" />
					<Grid ColumnSpacing="16">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<Button
							VerticalAlignment="Center"
							Click="ChangeIconButton_Click"
							Content="{x:Bind ChangeIconLabel}" />
						<Image
							x:Name="FolderIconImage"
							Grid.Column="1"
							Width="48"
							Height="48"
							HorizontalAlignment="Left" />
					</Grid>
				</StackPanel>
			</Border>
		</StackPanel>

	</ScrollViewer>

</UserControl>
