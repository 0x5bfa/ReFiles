<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<Window
	x:Class="Files.ItemProperties.ItemPropertiesWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:localization="using:Files.Localization"
	xmlns:local="using:Files.ItemProperties">

	<Window.SystemBackdrop>
		<MicaBackdrop />
	</Window.SystemBackdrop>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<InfoBar
			x:Name="ErrorInfoBar"
			Grid.Row="0"
			BorderThickness="0"
			CornerRadius="0"
			IsClosable="True"
			IsOpen="False"
			Severity="Error" />

		<ScrollViewer
			Grid.Row="1"
			HorizontalScrollBarVisibility="Disabled"
			HorizontalScrollMode="Enabled"
			VerticalScrollBarVisibility="Disabled"
			VerticalScrollMode="Disabled">
			<SelectorBar x:Name="PropertyPageSelector" SelectionChanged="PropertyPageSelector_SelectionChanged" />
		</ScrollViewer>

		<Grid x:Name="PropertyPagePanel" Grid.Row="2" />

		<Grid
			Grid.Row="3"
			Padding="12"
			HorizontalAlignment="Stretch"
			Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="0,1,0,0"
			ColumnSpacing="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Button
				Grid.Column="0"
				MinWidth="80"
				HorizontalAlignment="Stretch"
				Click="OkButton_Click"
				Content="{localization:Localized ResourceKey=Ok}" />
			<Button
				Grid.Column="1"
				MinWidth="80"
				HorizontalAlignment="Stretch"
				Click="CancelButton_Click"
				Content="{localization:Localized ResourceKey=Cancel}" />
			<Button
				Grid.Column="2"
				MinWidth="80"
				HorizontalAlignment="Stretch"
				Click="ApplyButton_Click"
				Content="{localization:Localized ResourceKey=Apply}"
				IsEnabled="{x:Bind ViewModel.HasChanges, Mode=OneWay}" />
		</Grid>
	</Grid>
</Window>
