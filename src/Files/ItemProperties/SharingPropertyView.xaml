<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.ItemProperties.SharingPropertyView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:localization="using:Files.Localization"
	xmlns:local="using:Files.ItemProperties">

	<ScrollViewer Padding="12,0,12,12">

		<StackPanel Spacing="12">
			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}">
				<StackPanel Spacing="12">
					<TextBlock FontWeight="SemiBold" Text="{localization:Localized ResourceKey=NetworkFolderSharing}" />
					<Grid ColumnSpacing="12">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<Image
							x:Name="FolderIconImage"
							Width="32"
							Height="32" />
						<StackPanel Grid.Column="1" Spacing="2">
							<TextBlock Text="{x:Bind Sharing.DisplayName}" TextTrimming="CharacterEllipsis" />
							<TextBlock Text="{x:Bind SharingState}" />
						</StackPanel>
					</Grid>
					<Grid ColumnSpacing="12" Visibility="{x:Bind NetworkPathVisibility}">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="120" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<TextBlock Text="{localization:Localized ResourceKey=NetworkPath}" />
						<TextBlock
							Grid.Column="1"
							Text="{x:Bind Sharing.NetworkPath}"
							TextTrimming="CharacterEllipsis" />
					</Grid>
					<Button
						HorizontalAlignment="Left"
						Click="ShareButton_Click"
						Content="{localization:Localized ResourceKey=Share}" />
				</StackPanel>
			</Border>

			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}">
				<StackPanel Spacing="12">
					<TextBlock FontWeight="SemiBold" Text="{localization:Localized ResourceKey=AdvancedSharingSection}" />
					<TextBlock Text="{localization:Localized ResourceKey=AdvancedSharingDescription}" TextWrapping="Wrap" />
					<Button HorizontalAlignment="Left" Click="AdvancedSharingButton_Click">
						<StackPanel Orientation="Horizontal" Spacing="6">
							<local:ElevationShieldIcon />
							<TextBlock Text="{localization:Localized ResourceKey=AdvancedSharing}" />
						</StackPanel>
					</Button>
				</StackPanel>
			</Border>

			<Border
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}"
				Visibility="{x:Bind PasswordProtectionVisibility}">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{localization:Localized ResourceKey=PasswordProtection}" />
					<TextBlock Text="{x:Bind PasswordProtectionDescription}" TextWrapping="Wrap" />
					<TextBlock TextWrapping="Wrap">
						<Run Text="{localization:Localized ResourceKey=PasswordProtectionSettingsPrefix}" />
						<Hyperlink Click="NetworkAndSharingCenterLink_Click">
							<Run Text="{localization:Localized ResourceKey=NetworkAndSharingCenter}" />
						</Hyperlink>
					</TextBlock>
				</StackPanel>
			</Border>
		</StackPanel>

	</ScrollViewer>

</UserControl>
