<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.ItemProperties.CompatibilityPropertyView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:localization="using:Files.Localization">
	<ScrollViewer Padding="14,12,14,8">
		<StackPanel Spacing="14">
			<TextBlock Text="{localization:Localized ResourceKey=CompatibilityIntro}" TextWrapping="Wrap" />
			<Button
				HorizontalAlignment="Left"
				Click="TroubleshooterButton_Click"
				Content="{localization:Localized ResourceKey=RunCompatibilityTroubleshooter}" />
			<Border
				Padding="10"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{localization:Localized ResourceKey=CompatibilityMode}" />
					<CheckBox Content="{localization:Localized ResourceKey=RunInCompatibilityMode}" IsEnabled="False" />
					<ComboBox
						HorizontalAlignment="Stretch"
						IsEnabled="False"
						ItemsSource="{x:Bind CompatibilityVersions}"
						SelectedIndex="0" />
				</StackPanel>
			</Border>
			<Border
				Padding="10"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1">
				<StackPanel Spacing="8">
					<TextBlock FontWeight="SemiBold" Text="{localization:Localized ResourceKey=CompatibilitySettings}" />
					<CheckBox Content="{localization:Localized ResourceKey=ReducedColorMode}" IsEnabled="False" />
					<CheckBox Content="{localization:Localized ResourceKey=RunIn640By480}" IsEnabled="False" />
					<CheckBox Content="{localization:Localized ResourceKey=DisableFullscreenOptimizations}" IsEnabled="False" />
					<CheckBox Content="{localization:Localized ResourceKey=RunAsAdministrator}" IsEnabled="False" />
					<Button
						HorizontalAlignment="Left"
						Click="HighDpiButton_Click"
						Content="{localization:Localized ResourceKey=ChangeHighDpiSettings}" />
				</StackPanel>
			</Border>
			<Grid ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<TextBlock Text="{localization:Localized ResourceKey=Program}" />
				<TextBlock
					Grid.Column="1"
					IsTextSelectionEnabled="True"
					Text="{x:Bind Compatibility.ExecutablePath}"
					TextTrimming="CharacterEllipsis" />
			</Grid>
		</StackPanel>
	</ScrollViewer>
</UserControl>
