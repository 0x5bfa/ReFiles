<!--  Copyright (c) Files Community. SPDX-License-Identifier: MPL-2.0  -->
<UserControl
	x:Class="Files.ItemProperties.HardwarePropertyView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Files.ItemProperties">

	<ScrollViewer Padding="12,0,12,12">

		<StackPanel Spacing="14">

			<TextBlock Text="{x:Bind AllDiskDrivesLabel}" TextWrapping="Wrap" />

			<StackPanel
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}">

				<Grid Padding="16,8" ColumnSpacing="12">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="2*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<TextBlock
						Grid.Column="1"
						FontWeight="SemiBold"
						Text="{x:Bind DeviceNameLabel}" />
					<TextBlock
						Grid.Column="2"
						FontWeight="SemiBold"
						Text="{x:Bind DeviceTypeLabel}" />
				</Grid>

				<ListView
					x:Name="HardwareList"
					Height="210"
					ItemsSource="{x:Bind Devices}"
					SelectionChanged="HardwareList_SelectionChanged"
					SelectionMode="Single">
					<ListView.ItemTemplate>
						<DataTemplate x:DataType="local:HardwareDeviceViewModel">
							<Grid ColumnSpacing="12">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="20" />
									<ColumnDefinition Width="2*" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<Image Width="16" Height="16" Source="{x:Bind Icon, Mode=OneWay}" />
								<TextBlock
									Grid.Column="1"
									Text="{x:Bind Name}"
									TextTrimming="CharacterEllipsis" />
								<TextBlock
									Grid.Column="2"
									Text="{x:Bind Type}"
									TextTrimming="CharacterEllipsis" />
							</Grid>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>

			</StackPanel>

			<StackPanel
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				CornerRadius="{StaticResource OverlayCornerRadius}">

				<Grid ColumnSpacing="12" RowSpacing="8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="120" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<TextBlock Text="{x:Bind ManufacturerLabel}" />
					<TextBlock
						x:Name="ManufacturerValue"
						Grid.Column="1"
						TextTrimming="CharacterEllipsis" />
					<TextBlock Grid.Row="1" Text="{x:Bind LocationLabel}" />
					<TextBlock
						x:Name="LocationValue"
						Grid.Row="1"
						Grid.Column="1"
						TextTrimming="CharacterEllipsis" />
					<TextBlock Grid.Row="2" Text="{x:Bind StatusLabel}" />
					<TextBlock
						x:Name="StatusValue"
						Grid.Row="2"
						Grid.Column="1"
						TextWrapping="Wrap" />
				</Grid>
				<Button
					x:Name="PropertiesButton"
					HorizontalAlignment="Right"
					Click="PropertiesButton_Click"
					Content="{x:Bind PropertiesLabel}"
					IsEnabled="False" />
			</StackPanel>

		</StackPanel>

	</ScrollViewer>

</UserControl>
